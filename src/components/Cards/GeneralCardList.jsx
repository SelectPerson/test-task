import React from 'react';

const GeneralCardList = ({ children }) => {
  return (
    <div className="general-card-list container">
      {children}
    </div>
  );
};

export default GeneralCardList;